/**
 * Placeholder para notificações em tempo real.
 * No legado subscreve notifications do Supabase; aqui no-op até integrar realtime.
 */
export function useRealtimeNotifications(_userId: string | undefined): void {
  // No-op: evita quebrar useAuth; pode ser implementado depois com Supabase Realtime
}
